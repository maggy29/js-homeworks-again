import InfiniteScroll from 'infinite-scroll';
const refs = {
    list: document.querySelector('.js-list'),
};
const options = {
    path(){
        this.pageIndex
        return 
    },  
    append: ,
    history:,
}

const infScrollInstance = new InfiniteScroll(refs.list, options)